<?php
$this->headTitle('List');

$order_by = $this->order_by;
$order    = $this->order;


$url_order = 'ASC';  // the following lines
if ($order_by == 'title') {
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';
} elseif ($order_by == 'artist') {
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';
}
?>

<table class="table">
    <tr>
        <th>
            <a href="<?php echo $this->url('list', array('order_by' => 'title', 'order' => $url_order)); ?>">Title <?php if ($order_by == 'title'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a>
        </th>
        <th>
            <a href="<?php echo $this->url('list', array('order_by' => 'artist', 'order' => $url_order)); ?>">Artist <?php if ($order_by == 'artist'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a></th>
        <th>&nbsp;</th>
    </tr>

</table>
